---
name: backlog
description: List and inspect the repository's checkpoint-oriented Agentflow backlog. Use when the user asks what work exists, remains, or should run next.
---

# Backlog

Run `./agentflow backlog` on POSIX or `.\agentflow.ps1 backlog` on Windows. Recommend the highest-priority unfinished task whose dependencies are ready; do not start it unless requested.
