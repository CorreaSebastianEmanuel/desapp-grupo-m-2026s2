---
name: history
description: Show the persistent feedback, agent handoffs, and workflow stage results for an Agentflow task. Use when a user asks what agents decided, how a task evolved, which feedback was incorporated, or why it is blocked.
---

# Show task history

Run `./agentflow history $ARGUMENTS` on POSIX or `.\agentflow.ps1 history $ARGUMENTS` on Windows. Treat tracked feedback and handoffs as the durable source of truth; runtime logs are supporting evidence.
