---
name: status
description: Show the current state and active run of an Agentflow task. Use for progress or status questions about a TASK identifier.
---

# Task status

Run `./agentflow status $ARGUMENTS` on POSIX or `.\agentflow.ps1 status $ARGUMENTS` on Windows and report its output without changing files.
